# ALC DSH adapter

Registers the bundled ALC Skill and exposes the AC Foundation LLM bridge to
ALC's private runtime. `llm-bridge.js` is a generated, checksum-verified copy
of the canonical Foundation implementation pinned by `runtime-sources.json`.
